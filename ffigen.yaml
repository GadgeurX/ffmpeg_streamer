# Run with `dart run ffigen --config ffigen.yaml`.
name: FfmpegStreamerBindings
description: |
  Bindings for `src/ffmpeg_streamer.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/ffmpeg_streamer_bindings_generated.dart'
headers:
  entry-points:
    - 'src/ffmpeg_streamer.h'
  include-directives:
    - 'src/ffmpeg_streamer.h'
preamble: |
  // ignore_for_file: always_specify_types
  // ignore_for_file: camel_case_types
  // ignore_for_file: non_constant_identifier_names
comments:
  style: any
  length: full
